#!/bin/env bash

# Check if user is logged in
if tailscale status | grep -qE '^Logged out.$' ; then
    sudo tailscale login || { echo "Failed to login to tailscale" ; exit 1 ; }
fi
sudo tailscale up --accept-routes
